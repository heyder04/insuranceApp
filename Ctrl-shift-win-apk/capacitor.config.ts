import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Ctrl-shift-win',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
